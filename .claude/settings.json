{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash|PowerShell",
        "hooks": [
          {
            "type": "command",
            "command": "python -c \"import runpy,subprocess; from pathlib import Path; runpy.run_path(str(Path(subprocess.check_output(['git','rev-parse','--show-toplevel'],text=True).strip())/'scripts/gate_commit.py'),run_name='__main__')\"",
            "timeout": 300
          }
        ]
      }
    ]
  }
}
